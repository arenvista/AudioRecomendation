[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.13.0",
    "datasets>=4.8.4",
    "evaluate>=0.4.6",
    "librosa>=0.11.0",
    "manim>=0.20.1",
    "matplotlib>=3.10.8",
    "pandas>=3.0.2",
    "prompt-toolkit>=3.0.52",
    "questionary>=2.1.1",
    "scikit-learn>=1.8.0",
    "tinytag>=2.2.1",
    "torch>=2.11.0",
    "torchcodec>=0.11.1",
    "tqdm>=4.67.3",
    "transformers>=5.5.0",
]

[tool.setuptools]
packages = ["audioencoder"]
package-dir = {"" = "src"}
